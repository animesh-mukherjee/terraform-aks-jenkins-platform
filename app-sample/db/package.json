{
  "name": "migrations",
  "version": "1.0.0",
  "description": "DB migration runner for platform-sample-app — runs inside ACI (Jenkinsfile.build Stage 4)",
  "main": "migrate.js",
  "dependencies": {
    "pg": "^8.11.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
